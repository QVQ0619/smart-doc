# 配置包(只读库页) 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在「配置包」页(`lib-packs`)按规则文件只读浏览审查配置包——每个规则文件一个包,展开看包内全部规则,不可修改。

**Architecture:** 方案 A 纯派生:配置包是现有规则的只读投影,不建表、不迁移、无写路径。后端新增一个只读聚合接口 `GET /api/config-packages`(在 `list_rules` 同款 join 上按文档分组);前端新增只读组件 `ConfigPackageLibrary`,详情复用现成 `GET /standard-docs/{doc_id}/rules`。

**Tech Stack:** 后端 Python 3.13 / FastAPI / SQLModel / SQLAlchemy,pytest 连真实 `smart_test` 库;前端 React 19 / antd 5 / @tanstack/react-query / vitest + Testing Library。

## Global Constraints

- **纯派生**:不新建数据库表、不做迁移、无任何写入/同步逻辑。
- **只读**:配置包页全程无 上传/新建/编辑/删除 按钮与接口。
- **后端 GET 不鉴权**:`GET /config-packages` 与现有 `GET /standard-docs/.../rules`、`/clauses` 一致,不挂 `require_api_key`。
- **归属/计数口径与 `list_rules` 完全一致**:join 链
  `standard_doc → regulation_clause(standard_doc_id) → review_rule_clause(clause_id) → review_rule_version(id==rule_version_id) → review_rule(current_version_id==version.id) → review_dimension`,
  且过滤 `standard_doc.is_active == True` 与 `review_rule.is_active == True`。
- **只含有效规则的文档才作为包返回**;无任何有效规则的文档不出现。
- **中文标签沿用现有映射**:判定 `hard/verify/soft → 硬性/需核验/建议`;处置 `reject/fix/review → 驳回/补正/复核`;绑定 `common/parameterized/specific → 通用/参数化/特定`;维度名直接用后端 `dimension_name`。
- **出处格式**:`第{page_no}页第{block_index+1}段`(`locator.block_index` 或 `para_index`;无段号则只 `第X页`;都无则 `—`)。
- **不退步**:后端 pytest 现有 **123** 全绿基础上新增本期用例;前端 vitest 现有 **47** 全绿基础上新增;`npm run build` 零类型错误。

## 文件结构

| 文件 | 职责 | 动作 |
|---|---|---|
| `backend/app/schemas.py` | 加 `ConfigPackageOut` 响应模型 | 修改 |
| `backend/app/routers/config_packages.py` | `GET /config-packages` 只读聚合 | 新建 |
| `backend/app/main.py` | 注册新路由 | 修改 |
| `backend/tests/test_config_packages.py` | 接口测试(连 `smart_test`) | 新建 |
| `src/api/configPackages.ts` | `ConfigPackage` 类型 + `listConfigPackages()` | 新建 |
| `src/api/configPackages.test.ts` | api 客户端测试 | 新建 |
| `src/components/ConfigPackageLibrary.tsx` | 只读列表 + 展开复用 `listRules` | 新建 |
| `src/components/ConfigPackageLibrary.test.tsx` | 组件测试 | 新建 |
| `src/pages/library/ConfigPacksPage.tsx` | 占位空页换成渲染组件 | 修改 |
| `src/pages/library/LibraryPages.test.tsx` | 加 ConfigPacksPage 渲染断言 | 修改 |

---

### Task 1: 后端 `GET /api/config-packages` 只读聚合接口

**Files:**
- Modify: `backend/app/schemas.py`(在 `RuleOut` 之后追加 `ConfigPackageOut`)
- Create: `backend/app/routers/config_packages.py`
- Modify: `backend/app/main.py:11,31`(import 与 include_router)
- Test: `backend/tests/test_config_packages.py`

**Interfaces:**
- Produces:
  - `ConfigPackageOut(doc_id:int, doc_code:str, title:str, version:str, rule_count:int, dimensions:list[str])`
  - `GET /api/config-packages -> list[ConfigPackageOut]`(200;无包返回 `[]`)
- Consumes:复用现有 `GET /api/standard-docs/{doc_id}/rules`(前端 Task 3 用),本任务不改它。

- [ ] **Step 1: 写失败测试 `backend/tests/test_config_packages.py`**

```python
from sqlalchemy import text
from sqlmodel import Session

from app.db import engine
from app.models import FileObject, ParseSegment, RegulationClause, StandardDoc


def _seed_doc(doc_code: str, title: str = "政策A") -> int:
    """建一个有效 standard_doc，返回 doc_id。"""
    with Session(engine) as s:
        fo = FileObject(bucket="local", object_key="k", file_name="f.pdf", size_bytes=1)
        s.add(fo); s.flush()
        sd = StandardDoc(doc_code=doc_code, title=title, version="V1.0", file_id=fo.id, is_active=True)
        s.add(sd); s.commit()
        return sd.id


def _add_clause(doc_id: int, doc_code: str, clause_no: str, page: int = 1) -> int:
    with Session(engine) as s:
        seg = ParseSegment(standard_doc_id=doc_id, page_no=page, locator={"page": page, "block_index": 0},
                           segment_type="text", content_text="x")
        s.add(seg); s.flush()
        c = RegulationClause(standard_doc_id=doc_id, doc_code=doc_code, clause_no=clause_no,
                             clause_text="文本", source_segment_id=seg.id)
        s.add(c); s.commit()
        return c.id


def _rule(clause_id: int, dimension: str, name: str) -> dict:
    return {"source_clause_id": clause_id, "dimension_code": dimension, "name": name,
            "logic": "l", "decision_type": "hard", "disposition": "reject", "binding_class": "common"}


def test_lists_doc_with_rules(client):
    doc_id = _seed_doc("SD-cfg1", "申请规定")
    c1 = _add_clause(doc_id, "SD-cfg1", "一")
    client.post(f"/api/standard-docs/{doc_id}/rules", json={"rules": [_rule(c1, "compliance", "规则A")]})
    r = client.get("/api/config-packages")
    assert r.status_code == 200
    rows = r.json()
    assert len(rows) == 1
    p = rows[0]
    assert p["doc_id"] == doc_id
    assert p["doc_code"] == "SD-cfg1"
    assert p["title"] == "申请规定"
    assert p["version"] == "V1.0"
    assert p["rule_count"] == 1
    assert p["dimensions"] == ["合规性"]


def test_doc_without_rules_excluded(client):
    _seed_doc("SD-cfg2")  # 有文档无规则
    r = client.get("/api/config-packages")
    assert r.status_code == 200
    assert r.json() == []


def test_inactive_rule_excluded(client):
    doc_id = _seed_doc("SD-cfg3")
    c1 = _add_clause(doc_id, "SD-cfg3", "一")
    client.post(f"/api/standard-docs/{doc_id}/rules", json={"rules": [_rule(c1, "compliance", "规则A")]})
    with Session(engine) as s:
        s.execute(text("UPDATE review_rule SET is_active = 0"))
        s.commit()
    r = client.get("/api/config-packages")
    assert r.json() == []  # 唯一规则停用 → 包消失


def test_rule_count_and_dimensions_aggregate(client):
    doc_id = _seed_doc("SD-cfg4")
    c1 = _add_clause(doc_id, "SD-cfg4", "一")
    c2 = _add_clause(doc_id, "SD-cfg4", "二")
    client.post(f"/api/standard-docs/{doc_id}/rules", json={"rules": [
        _rule(c1, "compliance", "规则A"),
        _rule(c2, "completeness", "规则B"),
    ]})
    p = client.get("/api/config-packages").json()[0]
    assert p["rule_count"] == 2
    assert set(p["dimensions"]) == {"合规性", "完整性"}


def test_empty_returns_empty_list(client):
    r = client.get("/api/config-packages")
    assert r.status_code == 200
    assert r.json() == []
```

- [ ] **Step 2: 运行测试,确认失败**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest tests/test_config_packages.py -v`
Expected: FAIL —— 404(路由不存在)/ collection 通过但断言失败。

- [ ] **Step 3: 加 schema `ConfigPackageOut`**

在 `backend/app/schemas.py` 的 `RuleOut` 类**之后**追加(文件顶部已 `from typing import ...`、`from pydantic import BaseModel`,无需新增 import):

```python
class ConfigPackageOut(BaseModel):
    doc_id: int
    doc_code: str
    title: str
    version: str
    rule_count: int
    dimensions: list[str]
```

- [ ] **Step 4: 新建路由 `backend/app/routers/config_packages.py`**

```python
from fastapi import APIRouter, Depends
from sqlalchemy import select
from sqlmodel import Session

from ..db import get_session
from ..models import (RegulationClause, ReviewDimension, ReviewRule,
                      ReviewRuleClause, ReviewRuleVersion, StandardDoc)
from ..schemas import ConfigPackageOut

router = APIRouter(tags=["config-packages"])


@router.get("/config-packages", response_model=list[ConfigPackageOut])
def list_config_packages(db: Session = Depends(get_session)) -> list[ConfigPackageOut]:
    """每个含有效规则的 standard_doc = 一个只读配置包。

    口径与 routers.clauses.list_rules 完全一致:同一条 join、同样 is_active 过滤;
    在文档维度上 GROUP BY,规则数取 distinct review_rule.id,维度去重保序。
    """
    rows = db.execute(
        select(
            StandardDoc.id, StandardDoc.doc_code, StandardDoc.title, StandardDoc.version,
            ReviewRule.id, ReviewDimension.name,
        )
        .join(RegulationClause, RegulationClause.standard_doc_id == StandardDoc.id)
        .join(ReviewRuleClause, ReviewRuleClause.clause_id == RegulationClause.id)
        .join(ReviewRuleVersion, ReviewRuleVersion.id == ReviewRuleClause.rule_version_id)
        .join(ReviewRule, ReviewRule.current_version_id == ReviewRuleVersion.id)
        .join(ReviewDimension, ReviewDimension.id == ReviewRuleVersion.dimension_id)
        .where(StandardDoc.is_active == True, ReviewRule.is_active == True)  # noqa: E712
        .order_by(StandardDoc.id)
    ).all()

    packages: dict[int, dict] = {}
    for doc_id, doc_code, title, version, rule_id, dim_name in rows:
        pkg = packages.get(doc_id)
        if pkg is None:
            pkg = {"doc_id": doc_id, "doc_code": doc_code, "title": title,
                   "version": version, "rule_ids": set(), "dimensions": []}
            packages[doc_id] = pkg
        pkg["rule_ids"].add(rule_id)
        if dim_name not in pkg["dimensions"]:
            pkg["dimensions"].append(dim_name)

    return [
        ConfigPackageOut(
            doc_id=p["doc_id"], doc_code=p["doc_code"], title=p["title"],
            version=p["version"], rule_count=len(p["rule_ids"]), dimensions=p["dimensions"],
        )
        for p in packages.values()
    ]
```

- [ ] **Step 5: 注册路由 `backend/app/main.py`**

把第 11 行
```python
from .routers import standard_docs, clauses
```
改为
```python
from .routers import standard_docs, clauses, config_packages
```
在第 31 行 `app.include_router(clauses.router, prefix="/api")` **之后**加一行:
```python
    app.include_router(config_packages.router, prefix="/api")
```

- [ ] **Step 6: 运行本任务测试,确认通过**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest tests/test_config_packages.py -v`
Expected: 5 passed。

- [ ] **Step 7: 跑后端全量,确认不退步**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest -q`
Expected: `128 passed`(原 123 + 本任务 5)。

- [ ] **Step 8: 提交**

```bash
git add backend/app/schemas.py backend/app/routers/config_packages.py backend/app/main.py backend/tests/test_config_packages.py
git commit -m "feat(api): GET /config-packages 只读配置包聚合(按规则文件派生)"
```

---

### Task 2: 前端 API 客户端 `configPackages.ts`

**Files:**
- Create: `src/api/configPackages.ts`
- Test: `src/api/configPackages.test.ts`

**Interfaces:**
- Produces:
  - `interface ConfigPackage { doc_id:number; doc_code:string; title:string; version:string; rule_count:number; dimensions:string[] }`
  - `listConfigPackages(): Promise<ConfigPackage[]>` —— GET `/api/config-packages`
- Consumes:Task 1 的 `GET /api/config-packages` 响应形状。

- [ ] **Step 1: 写失败测试 `src/api/configPackages.test.ts`**

```ts
import { afterEach, beforeEach, expect, test, vi } from "vitest";
import { listConfigPackages } from "./configPackages";

const fetchMock = vi.fn();
beforeEach(() => {
  vi.stubGlobal("fetch", fetchMock);
  fetchMock.mockReset();
});
afterEach(() => {
  vi.unstubAllGlobals();
});

test("listConfigPackages 请求配置包列表并解析", async () => {
  fetchMock.mockResolvedValue({
    ok: true, status: 200,
    json: async () => [
      { doc_id: 3, doc_code: "SD-x", title: "t", version: "V1.0", rule_count: 2, dimensions: ["合规性"] },
    ],
  });
  const res = await listConfigPackages();
  expect(fetchMock).toHaveBeenCalledWith("/api/config-packages");
  expect(res[0].rule_count).toBe(2);
  expect(res[0].dimensions).toEqual(["合规性"]);
});

test("非 2xx 抛错", async () => {
  fetchMock.mockResolvedValue({ ok: false, status: 500, statusText: "err", text: async () => "boom" });
  await expect(listConfigPackages()).rejects.toThrow(/500/);
});
```

- [ ] **Step 2: 运行测试,确认失败**

Run: `npx vitest run src/api/configPackages.test.ts`
Expected: FAIL —— 找不到模块 `./configPackages`。

- [ ] **Step 3: 实现 `src/api/configPackages.ts`**

```ts
export interface ConfigPackage {
  doc_id: number;
  doc_code: string;
  title: string;
  version: string;
  rule_count: number;
  dimensions: string[];
}

async function handle<T>(res: Response): Promise<T> {
  if (!res.ok) {
    const text = await res.text().catch(() => "");
    throw new Error(`HTTP ${res.status}: ${text || res.statusText}`);
  }
  return (await res.json()) as T;
}

// 只读 GET，无需鉴权头（与后端 GET /config-packages 一致）
export function listConfigPackages(): Promise<ConfigPackage[]> {
  return fetch("/api/config-packages").then((r) => handle<ConfigPackage[]>(r));
}
```

- [ ] **Step 4: 运行测试,确认通过**

Run: `npx vitest run src/api/configPackages.test.ts`
Expected: 2 passed。

- [ ] **Step 5: 提交**

```bash
git add src/api/configPackages.ts src/api/configPackages.test.ts
git commit -m "feat(web): configPackages api 客户端 listConfigPackages"
```

---

### Task 3: 只读组件 `ConfigPackageLibrary`

**Files:**
- Create: `src/components/ConfigPackageLibrary.tsx`
- Test: `src/components/ConfigPackageLibrary.test.tsx`

**Interfaces:**
- Consumes:
  - `listConfigPackages()` / `ConfigPackage`(Task 2)
  - 现有 `listRules(docId)` / `Rule`(`src/api/standardDocs.ts`)
- Produces:`export default function ConfigPackageLibrary()`(供 Task 4 页面渲染)。

说明:空状态用 antd Table 的 `locale.emptyText` 承载设计文档的空文案(与 `StandardDocLibrary` 保持一致的表格观感),用户可见结果与设计一致。

- [ ] **Step 1: 写失败测试 `src/components/ConfigPackageLibrary.test.tsx`**

```tsx
import { render, screen, waitFor } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import ConfigPackageLibrary from "./ConfigPackageLibrary";
import * as pkgApi from "../api/configPackages";
import * as docApi from "../api/standardDocs";

vi.mock("../api/configPackages", async () => {
  const actual = await vi.importActual<typeof pkgApi>("../api/configPackages");
  return { ...actual, listConfigPackages: vi.fn() };
});
vi.mock("../api/standardDocs", async () => {
  const actual = await vi.importActual<typeof docApi>("../api/standardDocs");
  return { ...actual, listRules: vi.fn() };
});

function renderLib() {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return render(
    <QueryClientProvider client={qc}>
      <ConfigPackageLibrary />
    </QueryClientProvider>,
  );
}

const pkg = { doc_id: 3, doc_code: "SD-x", title: "申请规定", version: "V1.0", rule_count: 1, dimensions: ["合规性"] };

beforeEach(() => {
  vi.mocked(pkgApi.listConfigPackages).mockResolvedValue([pkg] as never);
  vi.mocked(docApi.listRules).mockResolvedValue([
    {
      id: 1, rule_code: "RULE-a", version: "V1.0", name: "同年限申请1项", logic: null,
      dimension_code: "compliance", dimension_name: "合规性", decision_type: "hard",
      disposition: "reject", binding_class: "common", source_clause_id: 9, clause_no: "二",
      clause_text: "x", page_no: 5, locator: { page: 5, block_index: 0 },
    },
  ] as never);
});

test("渲染配置包列表（名称/编码/维度）", async () => {
  renderLib();
  expect(await screen.findByText("申请规定")).toBeInTheDocument();
  expect(screen.getByText("SD-x")).toBeInTheDocument();
  expect(screen.getByText("合规性")).toBeInTheDocument();
});

test("展开配置包行只读列出规则与出处", async () => {
  const { container } = renderLib();
  await screen.findByText("申请规定");
  const expandBtn = container.querySelector(".ant-table-row-expand-icon") as HTMLElement;
  await userEvent.click(expandBtn);
  await waitFor(() => expect(vi.mocked(docApi.listRules)).toHaveBeenCalledWith(3));
  expect(await screen.findByText("同年限申请1项")).toBeInTheDocument();
  expect(screen.getByText("第5页第1段")).toBeInTheDocument();
});

test("整页无编辑/删除/上传按钮（只读）", async () => {
  renderLib();
  await screen.findByText("申请规定");
  expect(screen.queryByText("编辑")).not.toBeInTheDocument();
  expect(screen.queryByText("删除")).not.toBeInTheDocument();
  expect(screen.queryByRole("button", { name: /上传/ })).not.toBeInTheDocument();
});

test("无配置包时显示空状态文案", async () => {
  vi.mocked(pkgApi.listConfigPackages).mockResolvedValue([] as never);
  renderLib();
  expect(await screen.findByText(/暂无配置包/)).toBeInTheDocument();
});
```

- [ ] **Step 2: 运行测试,确认失败**

Run: `npx vitest run src/components/ConfigPackageLibrary.test.tsx`
Expected: FAIL —— 找不到模块 `./ConfigPackageLibrary`。

- [ ] **Step 3: 实现 `src/components/ConfigPackageLibrary.tsx`**

```tsx
import { Table, Tag } from "antd";
import type { ColumnsType } from "antd/es/table";
import { useQuery } from "@tanstack/react-query";
import { listRules, type Rule } from "../api/standardDocs";
import { listConfigPackages, type ConfigPackage } from "../api/configPackages";

const DECISION_LABEL: Record<string, string> = { hard: "硬性", verify: "需核验", soft: "建议" };
const DISPOSITION_LABEL: Record<string, string> = { reject: "驳回", fix: "补正", review: "复核" };
const BINDING_LABEL: Record<string, string> = { common: "通用", parameterized: "参数化", specific: "特定" };

function ruleProvenance(r: Rule): string {
  const loc = r.locator ?? {};
  const raw = loc["block_index"] ?? loc["para_index"];
  const segPart = typeof raw === "number" ? `第${raw + 1}段` : "";
  if (r.page_no != null) return `第${r.page_no}页${segPart}`;
  return segPart || "—";
}

const RULE_COLS: ColumnsType<Rule> = [
  { title: "规则名", dataIndex: "name", key: "name" },
  { title: "维度", dataIndex: "dimension_name", key: "dimension_name", width: 90 },
  { title: "判定", key: "decision", width: 90,
    render: (_: unknown, r: Rule) => <Tag>{DECISION_LABEL[r.decision_type] ?? r.decision_type}</Tag> },
  { title: "处置", key: "disposition", width: 90,
    render: (_: unknown, r: Rule) => <Tag>{DISPOSITION_LABEL[r.disposition] ?? r.disposition}</Tag> },
  { title: "绑定", key: "binding", width: 90,
    render: (_: unknown, r: Rule) => <Tag>{BINDING_LABEL[r.binding_class] ?? r.binding_class}</Tag> },
  { title: "出处", key: "prov", width: 160, render: (_: unknown, r: Rule) => ruleProvenance(r) },
];

// 展开区：只读列出该包(=该文档)的全部规则，复用现成 listRules
function PackageRules({ docId }: { docId: number }) {
  const q = useQuery({ queryKey: ["config-package-rules", docId], queryFn: () => listRules(docId) });
  if (q.isLoading) return <span>加载中…</span>;
  if (q.isError) return <span>规则加载失败</span>;
  const data = q.data ?? [];
  if (!data.length) return <span>该配置包暂无规则</span>;
  return <Table rowKey="id" size="small" dataSource={data} columns={RULE_COLS} pagination={false} />;
}

const PACKAGE_COLS: ColumnsType<ConfigPackage> = [
  { title: "配置包名称", dataIndex: "title", key: "title" },
  { title: "关联文件编码", dataIndex: "doc_code", key: "doc_code", width: 180 },
  { title: "版本", dataIndex: "version", key: "version", width: 90 },
  { title: "规则数", dataIndex: "rule_count", key: "rule_count", width: 90 },
  {
    title: "覆盖维度", key: "dimensions",
    render: (_: unknown, p: ConfigPackage) => <>{p.dimensions.map((d) => <Tag key={d}>{d}</Tag>)}</>,
  },
];

export default function ConfigPackageLibrary() {
  const listQuery = useQuery({ queryKey: ["config-packages"], queryFn: listConfigPackages });
  if (listQuery.isError) return <div>配置包加载失败</div>;
  return (
    <Table
      rowKey="doc_id"
      size="middle"
      loading={listQuery.isLoading}
      dataSource={listQuery.data ?? []}
      columns={PACKAGE_COLS}
      pagination={false}
      locale={{ emptyText: "暂无配置包，请先在规则库中为规则文件抽取审查规则" }}
      expandable={{ expandedRowRender: (p: ConfigPackage) => <PackageRules docId={p.doc_id} /> }}
    />
  );
}
```

- [ ] **Step 4: 运行测试,确认通过**

Run: `npx vitest run src/components/ConfigPackageLibrary.test.tsx`
Expected: 4 passed。

- [ ] **Step 5: 提交**

```bash
git add src/components/ConfigPackageLibrary.tsx src/components/ConfigPackageLibrary.test.tsx
git commit -m "feat(web): ConfigPackageLibrary 只读配置包列表+展开规则"
```

---

### Task 4: 挂进「配置包」页 + 全量验证

**Files:**
- Modify: `src/pages/library/ConfigPacksPage.tsx`(整文件替换)
- Modify: `src/pages/library/LibraryPages.test.tsx`(加 mock + import + 一条断言)

**Interfaces:**
- Consumes:`ConfigPackageLibrary`(Task 3)。
- Produces:`lib-packs` 路由可见的配置包页(App.tsx 已把 `lib-packs` 映射到 `ConfigPacksPage`,无需改路由)。

- [ ] **Step 1: 改页面测试 `src/pages/library/LibraryPages.test.tsx`**

在文件顶部已有的 `vi.mock("../../components/StandardDocLibrary", ...)` **之后**追加一个 mock:
```tsx
vi.mock("../../components/ConfigPackageLibrary", () => ({
  default: () => <div data-testid="config-pkg-lib" />,
}));
```
在 `import DocArchivePage from "./DocArchivePage";` 之后加:
```tsx
import ConfigPacksPage from "./ConfigPacksPage";
```
在文件末尾追加一条测试:
```tsx
test("配置包页渲染 ConfigPackageLibrary", () => {
  render(<ConfigPacksPage />);
  expect(screen.getByTestId("config-pkg-lib")).toBeInTheDocument();
});
```

- [ ] **Step 2: 运行该测试,确认失败**

Run: `npx vitest run src/pages/library/LibraryPages.test.tsx`
Expected: FAIL —— `config-pkg-lib` 未找到(页面还渲染的是占位 `EmptyState`)。

- [ ] **Step 3: 替换 `src/pages/library/ConfigPacksPage.tsx`**

```tsx
import PageScaffold from "../../components/PageScaffold";
import ConfigPackageLibrary from "../../components/ConfigPackageLibrary";

export default function ConfigPacksPage() {
  return (
    <PageScaffold title="配置包" subtitle="按规则文件浏览审查配置包（只读）">
      <ConfigPackageLibrary />
    </PageScaffold>
  );
}
```

- [ ] **Step 4: 运行该测试,确认通过**

Run: `npx vitest run src/pages/library/LibraryPages.test.tsx`
Expected: 3 passed(原 2 + 新 1)。

- [ ] **Step 5: 前端全量 + 构建**

Run: `npx vitest run`
Expected: 现有 47 + 本期 7(api 2 + 组件 4 + 页面 1)= `54 passed`。

Run: `npm run build`
Expected: 构建成功,零类型错误(仅既有 chunk 体积告警,与本期无关)。

- [ ] **Step 6: 后端全量复核**

Run: `cd backend && ./.venv/Scripts/python.exe -m pytest -q`
Expected: `128 passed`。

- [ ] **Step 7: 提交**

```bash
git add src/pages/library/ConfigPacksPage.tsx src/pages/library/LibraryPages.test.tsx
git commit -m "feat(web): 配置包页接入 ConfigPackageLibrary"
```

---

## 自检(Self-Review)

**Spec 覆盖**
- §4.1 `GET /config-packages`(无鉴权、只含有效规则、GROUP BY 文档、rule_count、dimensions、按 id 排序)→ Task 1 ✅
- §4.2 `ConfigPackageOut` → Task 1 Step 3 ✅
- §4.3 详情复用 `GET /standard-docs/{doc_id}/rules` → Task 3(`PackageRules`)✅
- §5.1 页面挂载 + `configPackages.ts` + 复用 `listRules` → Task 2 / 3 / 4 ✅
- §5.2 列表 5 列 + 整页只读 + 空状态文案 → Task 3 ✅
- §5.3 展开只读规则 + 出处 → Task 3 ✅
- §5.4 不抽公共组件、独立只读渲染、加载/失败处理 → Task 3(`PackageRules` 的 isLoading/isError)✅
- §6 错误处理(空 `[]`、列表 isError、展开 isError)→ Task 1 / Task 3 ✅
- §7 测试(后端 4 类 + 前端 4 类)→ Task 1 测试含 5 例(覆盖"有规则/无规则/停用/空"+ 聚合)、Task 3 含 4 例 ✅
- §8 N:M 技术债沿用 → 口径与 `list_rules` 一致,Task 1 注释已说明 ✅

**占位扫描**:无 TBD/TODO;所有步骤含完整代码与命令。

**类型一致性**:`ConfigPackageOut`/`ConfigPackage` 六字段(`doc_id/doc_code/title/version/rule_count/dimensions`)在后端 schema、前端 interface、组件列、两端测试中**完全一致**;`listConfigPackages` / `listRules` / `ConfigPackage` / `Rule` 命名跨任务统一;后端聚合 join 与 `list_rules`(`routers/clauses.py:122-130`)同构。

**计数口径**:`rule_count` = 每文档 distinct `review_rule.id`(`rule_ids` 用 `set`),与 N:M 重复行问题隔离;维度 `dimensions` 去重保序。
